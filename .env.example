# ============================================
# CRE Deal Management Platform - Environment Template
# ============================================
# Copy this file to .env and fill in your values
# NEVER commit .env files to git

# ============================================
# Kernel API (cre-kernel-phase1)
# ============================================
# PostgreSQL connection string
DATABASE_URL=postgresql://canonical:canonical-dev-password@localhost:5432/cre_kernel

# Test database (used by Vitest)
DATABASE_URL_TEST=postgresql://canonical:canonical-dev-password@localhost:5432/cre_kernel_test

# Kernel API port
PORT=3001

# ============================================
# BFF Server (canonical-deal-os/server)
# ============================================
# Kernel API URL (BFF proxies requests here)
KERNEL_API_URL=http://localhost:3001

# BFF server port
BFF_PORT=8787

# JWT secret for authentication (CHANGE IN PRODUCTION)
JWT_SECRET=dev-secret-change-this-in-production

# BFF SQLite database path (relative to server/)
BFF_DB_URL=file:./server/.data/llm-airlock.db

# ============================================
# Optional: OpenAI (for LLM features)
# ============================================
# BFF_OPENAI_API_KEY=sk-...
# BFF_OPENAI_MODEL=gpt-4o-mini

# ============================================
# Optional: LP Portal Email/Webhooks
# ============================================
# BFF_LP_INVITATION_EMAIL_ENDPOINT=
# BFF_LP_NOTIFICATION_WEBHOOK_URL=
# BFF_LP_NOTIFICATION_WEBHOOK_SECRET=

# ============================================
# Docker Compose (if using docker-compose.yml)
# ============================================
# These are pre-configured in docker-compose.yml
# Override them here if running outside Docker
